class AreaCalculator {
  const AreaCalculator();

  double calculateAreaSquareMeters(Iterable<Object> points) {
    throw UnimplementedError('Area calculation is implemented in Phase 6.');
  }

  double calculatePerimeterMeters(Iterable<Object> points) {
    throw UnimplementedError(
      'Perimeter calculation is implemented in Phase 6.',
    );
  }
}
